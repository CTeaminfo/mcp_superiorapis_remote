# Superior APIs MCP Server 環境變數配置
# 複製此檔案為 .env 並填入實際值

# =============================================================================
# Superior APIs 配置
# =============================================================================

# Superior APIs 認證 Token (不需要設定)
# 注意: MCP 伺服器作為代理，token 由客戶端在 HTTP header 中提供
# 伺服器端不需要配置 TOKEN，客戶端會在每個請求中提供
# TOKEN=your_superior_apis_token_here

# Superior APIs 基礎 URL (可選，預設為官方網址)
SUPERIOR_API_BASE=https://superiorapis-creator.cteam.com.tw

# Superior APIs 插件列表端點 (可選，使用預設路徑)
PLUGINS_LIST_ENDPOINT=/manager/module/plugins/list_v3

# =============================================================================
# 伺服器配置
# =============================================================================

# HTTP 伺服器端口 (預設: 8000，符合 MCP 社群建議)
HTTP_SERVER_PORT=8000

# SSE 伺服器端口 (預設: 8080，符合 MCP 社群建議)
SSE_SERVER_PORT=8080

# 伺服器主機地址 (預設: 0.0.0.0)
SERVER_HOST=0.0.0.0

# =============================================================================
# 開發和除錯配置
# =============================================================================

# 日誌級別 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# 開發模式 (true/false)
# 啟用時會有自動重載和詳細錯誤訊息
DEV_MODE=false

# 允許的來源 (CORS)，用逗號分隔
# 預設允許本地開發
ALLOWED_ORIGINS=http://localhost,http://127.0.0.1

# =============================================================================
# 快取配置
# =============================================================================

# 工具快取過期時間（秒）
# 設為 0 表示不快取，-1 表示永不過期
CACHE_EXPIRY=3600

# =============================================================================
# 測試配置
# =============================================================================

# 測試用的 MCP 伺服器 URL
TEST_MCP_SERVER_URL=http://localhost:8000/mcp

# 測試用的 SSE 伺服器 URL  
TEST_SSE_SERVER_URL=http://localhost:8080

# =============================================================================
# 安全配置
# =============================================================================

# 啟用 Origin 驗證 (true/false)
VALIDATE_ORIGIN=true

# Session 過期時間（秒）
SESSION_TIMEOUT=7200